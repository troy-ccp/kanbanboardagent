[[{"const": "fs = require('fs');\nconst path = require('path');\nconst { exec } = require('child_process');\n\n// Configuration\nconst API_URL = 'http://localhost:8880/v1/audio/speech';\nconst DEFAULT_VOICE = 'af_heart';\nconst DEFAULT_SPEED = 1.0;\n\n// Parse arguments\nconst args = process.argv.slice(2);\nif (args.length === 0) {\n  console.error('Usage: node speak.js <text> [voice] [speed]');\n  process.exit(1);"}, {}, {}, {}, {}, {}, {}, {"console.log(`Generated: ${filename}`);\n    console.log(`Text: \"${text}\"`);": "} catch (error) {\n    console.error('TTS Error:', error.message);\n    process.exit(1);\n  }\n}"}, {}], "audio...');", "console.error('Playback error:', error);\n      reject(error);\n    } else {\n      console.log('Playback complete!');\n      resolve();\n    }\n  });", {}, {}]